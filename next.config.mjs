// /** @type {import('next').NextConfig} */
// const nextConfig = {
//     images: {
//     domains: ['*'],
//   },
// };

// export default nextConfig;


/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['gaurav-salian.github.io'],
  },
};

export default nextConfig;
